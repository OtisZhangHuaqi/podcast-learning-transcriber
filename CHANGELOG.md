# Changelog

本项目从公开版本0.4.1开始维护此变更记录。

## 0.4.2

- Windows 与 macOS 的失败任务会写入按日期分割的结构化错误日志，并可从任务界面打开日志目录。
- 子进程失败会保留程序名称、退出码、终止信号和 stderr/stdout 尾部，同时自动遮蔽凭据和用户主目录。
- macOS Whisper 改为静态链接，避免打包后因 `libwhisper`/`libggml` 动态库加载路径失效而无法启动。
- macOS 原生工具使用 macOS 12 最低部署目标，并在构建阶段执行 Whisper 冒烟测试及动态库引用审计。
- Whisper 返回成功但没有生成有效 TXT/SRT 文件时，任务会明确失败并记录诊断日志。

## 0.4.1

- 失败任务可从原链接和设置重新加入队列。
- DeepSeek结构化步骤启用JSON Output、诊断记录和批次检查点。
- 单个字幕校正批次持续异常时保留原文并继续后续批次。
- 视频任务复用已有原始逐字稿，避免重复下载和Whisper转录。
- Windows与macOS Universal使用统一版本与发布说明。
